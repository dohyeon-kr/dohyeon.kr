{{!< default}}

<main class="site-main">
    <section class="archive-header">
        <p class="eyebrow">Error {{statusCode}}</p>
        <h1>{{message}}</h1>
        <p>요청한 페이지를 찾지 못했습니다.</p>
        <a class="button button--primary" href="{{@site.url}}">Go home</a>
    </section>
</main>
